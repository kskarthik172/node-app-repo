{
  "name": "sample-node-app",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --coverage",
    "build": "tar -czf app.tar.gz server.js package.json node_modules"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}

